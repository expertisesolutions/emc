ACLOCAL_AMFLAGS = -I m4
AM_MAKEFLAGS = --no-print-directory

SUBDIRS = src

all:
	cd themes/default/ && edje_cc default.edc
	cd src && make

debug:
	cd themes/default/ && edje_cc default.edc
	cd src && make debug

clean:
	cd src && make clean
